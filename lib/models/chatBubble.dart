class ChatBubbles {
  String msg;
  DateTime date;
  ChatBubbles(this.msg, this.date);
}

class Chats {
  String name, status;
  int mssg;
  Chats({required this.name, required this.status, required this.mssg});
}
